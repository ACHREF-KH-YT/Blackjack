# Assets Folder

This directory is reserved for application assets (images, icons, custom audio files).

Currently, all card graphics and tables are fully vector-rendered in code, and the dynamic retro blackjack sounds are synthesized in real-time using the browser's built-in Web Audio API. Therefore, no physical files are required in this folder for the game to operate.
